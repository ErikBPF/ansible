- name: install development packages
  tags:
    - full-install
    - light-install
    - packages
  kewlfft.aur.aur:
    name:
      - wget
      - base-devel
      - libsecret
      - openvpn
      - python-pip
      - net-tools
      - pkgfile
      - postgresql-libs
      - dbeaver
      - postman-bin
      - dashbinsh
      - unixodbc
      #     - azure-cli
      - visual-studio-code-insiders-bin
      - baobab
    use: yay
    state: present
    update_cache: false
  ignore_errors: false
